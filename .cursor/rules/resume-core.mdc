---
description: Resume-Agent 核心约束 — 事实边界与输出契约
alwaysApply: true
---

# Resume-Agent 核心规则

> **流程描述以 [../skills/resume-agent/SKILL.md](../skills/resume-agent/SKILL.md) 为单一事实源**。本规则只列禁令、契约、安全约束，不重述步骤详情。

## 事实边界

- 只能重组、润色、突出已有信息
- **禁止**捏造公司、职位、年限、项目成果、技能、证书
- 原文缺失量化数据时，使用 `[待补充: 具体描述]` 标记，不得编造数字

## 输出契约（8 项，缺一不可）

每次改简历任务必须产出：

1. `plans/YYYY-MM-DD-<name>.md` — 优化计划（Step 1）
2. `prompts/runtime/YYYY-MM-DD-<name>.interviewer.system.md` — 面试官系统提示词（Step 2）
3. `reviews/YYYY-MM-DD-<name>.md` + `.json` — 面试官评测报告与分数（Step 2）
4. `resumes/working/<name>.md` — 中间稿
5. `resumes/output/<name>.md` — 定稿（需用户确认后写入）
6. `evals/runs/YYYY-MM-DD-<name>-r{N}.json` — Step 4 面板汇总
7. `evals/panel/...` — Step 4 三子代理报告（每轮 × 3）
8. `gaps/YYYY-MM-DD-<name>.md` + `.json` — Step 5 JD 差距对照（**任务完成标志**）

## 文件保护

- **禁止**覆盖或删除 `resumes/input/` 中的文件
- **禁止**覆盖、删除或写入 `intake/` 中的文件（与 input 同级只读）
- **禁止**修改 `prompts/system/`（Harness 只读）
- **禁止**修改 `schemas/`（输出契约只读，变更须 ADR）
- **禁止**未经用户确认将 working 稿直接作为 output 定稿

## 输入门禁

- **招聘要求（JD）为必填项**：`intake/<name>/requirements.md` 必须存在且含有效 JD 内容
- **禁止**在无有效 JD 时生成 Plan、进入 Interview 或 Rewrite
- JD 缺失或无效时，Agent 须停止流程、明确告知用户并等待补充；**不得**用「未提供」占位完成 Plan
- **`intake/` 只读**：Agent 不得创建或修改 `intake/` 下任何文件；用户须自行落盘，Agent 仅读取

## 流程门禁（不得跳步）

- 不得跳过 Step 1 Plan
- 不得跳过 Step 2 Interview 直接 Rewrite
- 不得跳过 Step 4 Eval
- 不得 Step 4 通过却跳过 Step 5 结束任务
- **禁止**主 Agent 代替三子代理打分
- **禁止**未获 3/3 全票通过即进入 Step 5
- **禁止** Step 5 建议用户向简历编造不存在的技能/项目

详细执行顺序见 [SKILL.md](../skills/resume-agent/SKILL.md)。

## PII 安全

- eval 记录与 gap 报告中不得写入手机号、邮箱、身份证号等明文
- 使用 `<redacted>` 或 `[PII]` 替代敏感字段
- `validate-output.mjs` + `redact-pii.mjs` 由 hooks 自动触发；不得通过禁用 hook 绕过

## Schema 与 Provenance

- Step 2 / 4 / 5 输出的所有 JSON 须满足 [`schemas/`](../../schemas/) 中对应 schema
- 每份输出 JSON 须含 `provenance` 字段（`agent_model` / `temperature` / `seed` / `prompt_shas` / `generated_at`）

## Harness 变更门禁

- 改动 `.cursor/rules/`、`.cursor/hooks.json`、`schemas/`、`scripts/`、`prompts/system/` 前，须运行 `node scripts/check-doc-refs.mjs` 退出 0
- 改动须在 `docs/adr/` 留 ADR
- 不得通过禁用 hooks 或跳过 eval 来「通过」验收
